import { FacebookPageAuthGuard } from './facebook-page-auth.guard';

describe('FacebookPageAuthGuard', () => {
  it('should be defined', () => {
    expect(new FacebookPageAuthGuard()).toBeDefined();
  });
});
