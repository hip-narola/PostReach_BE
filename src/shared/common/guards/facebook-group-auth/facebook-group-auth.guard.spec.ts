import { FacebookGroupAuthGuard } from './facebook-group-auth.guard';

describe('FacebookGroupAuthGuard', () => {
  it('should be defined', () => {
    expect(new FacebookGroupAuthGuard()).toBeDefined();
  });
});
